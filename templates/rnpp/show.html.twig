{% extends 'base.html.twig' %}

{% block title %}Rnpp{% endblock %}

{% block body %}
    <h1>Rnpp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rnpp.id }}</td>
            </tr>
            <tr>
                <th>Npi</th>
                <td>{{ rnpp.npi }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ rnpp.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ rnpp.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ rnpp.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rnpp_index') }}" class="btn btn-primary" >Retour </a></br></br>

    <a href="{{ path('app_rnpp_edit', {'id': rnpp.id}) }}"> Editer </a></br></br>

    {{ include('rnpp/_delete_form.html.twig') }}
{% endblock %}
